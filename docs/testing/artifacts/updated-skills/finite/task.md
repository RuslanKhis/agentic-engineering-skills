$adk-engineer Our ADK chat sometimes displays duplicate answer text or reports success before a late upstream failure. Repair the finite answer adapter in this small existing project, using the installed skills' relevant implementation guidance. Keep provisional streaming feedback responsive while accepting only a complete result. This is an offline application-boundary change; no deployment or measured performance claim is needed.

`stream_adapter.collect_answer` already has callers. Preserve its async signature, the exported `AnswerRejected` exception, and the unrelated `application_config.py`. Keep the project standard-library-only on Python 3.11+. You may copy a suitable bundled skill asset into this project when helpful, retaining its license notice; wire it into the actual adapter rather than leaving unused example code.

The input is one finite asynchronous iterator of already decoded ADK event dictionaries. The application owns transport decoding and raw-byte admission. `invocation_id` and `answer_author` are trusted arguments, not values to discover from the incoming stream. Input partials are text deltas, not cumulative snapshots. Implement this contract:

1. Every event must match the trusted invocation ID. Accept the SDK's snake_case or camelCase spelling for invocation ID, finish reason and error fields. Only nonthought text from the designated model answer author can be shown or accepted. Tool-bearing events are protocol work, not answer text; this adapter does not verify their business results.
2. Await `on_preview` with the reconstructed text snapshot after each eligible partial delta. Previews are provisional and are never the return value merely because input ended. A complete nonpartial `STOP` answer replaces the preview; do not append the aggregate again or send it as another partial callback.
3. Return that complete answer only after the stream has exhausted cleanly. Consume all remaining events. Reject reported errors or transport failures even when they arrive after plausible final text. Reject missing completion, `MAX_TOKENS`, interruption and a mismatched invocation. There is exactly one completed answer in this profile.
4. This adapter owns the iterator it obtains. Await its `aclose()` when provided on success, rejection, callback failure and caller cancellation. A close failure prevents acceptance. Preserve caller cancellation as `asyncio.CancelledError`; other failures use `AnswerRejected` with a controlled message that does not disclose provider/callback/close exception content.

Keep this focused on a finite text-answer contract, preserving existing behavior outside it. Add meaningful offline tests of the actual public adapter and a short README with an executable test command. Run the tests before finishing. No external model calls, network use, cloud commands, package installation, commits or changes outside this project are needed or authorized. Report the files changed and actual checks.
