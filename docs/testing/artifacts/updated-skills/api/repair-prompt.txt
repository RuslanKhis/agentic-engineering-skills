Independent review of the generated adapter found two reproducible violations of CONTRACT.md. Fix these in the current project, preserve interfaces/dependencies, add focused regressions, and rerun your tests.

1. _log uses traceback.format_tb(error.__traceback__). That formatter includes source lines. A real local MockTransport handler source file containing raise RuntimeError("PRIVATE_EXCEPTION_PAYLOAD_CANARY") puts this literal into provider_call.error_frames on the captured LogRecord, even though the public error is safe. Log only safe structured stack locations such as filename/function/line number (no source line, locals, or exception message), or a safe stage. Test the entire structured log extras, not only LogRecord.getMessage(). Preserve useful error class and failure visibility.
2. The contract does not demand canonical amount spelling. At least "01.00" is a positive finite decimal string with exactly two fractional digits, but the generated regex rejects it before dispatch. Accept this valid input without normalizing the caller's submitted string; the provider reply must still match exactly. Signed positive decimal spelling "+1.00" is also valid here. Preserve bounded parsing and invalid/nonfinite/negative/zero/subcent rejection.

Do not inspect acceptance tests outside the project. Work only inside this project; all validation must use local doubles. No network, cloud, dependency or git operations. Use the existing temporary runtime interpreter as before. Complete the repair and report actual checks.
