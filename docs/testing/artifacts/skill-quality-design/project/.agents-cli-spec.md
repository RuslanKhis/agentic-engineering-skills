# Accepted design
A support assistant on Cloud Run. Store explicit user language preferences in the existing SQLite profile database for the local prototype. Serving code derives the user ID from authenticated middleware. Preserve the model and infrastructure.
