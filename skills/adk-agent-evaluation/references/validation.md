# Validation evidence

Initial checks completed on 15 September 2026; the 16 September depth review is recorded separately below. These are results for the packaged skill, separate from the historical ADK integration evidence in [compatibility.md](compatibility.md) and [provenance.md](provenance.md).

## Environments

| Environment | Actual configuration and scope |
| --- | --- |
| Primary | macOS 26.6.2, arm64; fresh CPython 3.11.4 virtual environment. Standard library for helper and fixture tests; PyYAML 6.0.3 from an existing offline cache for the official skill validator. |
| Second interpreter | CPython 3.12.14 on the same macOS host; isolated execution of the copied helper's standard-library tests. |
| Forward evaluation | Three fresh Codex subagents, without conversation history, using the standalone skill copy, isolated synthetic fixtures, realistic requests and an explicit no-install/no-network constraint. The primary interpreter had no ADK, GenAI SDK, Vertex SDK or pytest installed. |

The chapter's earlier virtual environment was absent. No companion dependency pin or existing environment was changed, and no network dependency installation was used. Historical ADK 2.8.0 execution is not a new result of these environments.

## Packaged helper and structure

The following command notation removes machine-specific paths: `PY` is the primary interpreter, `PY312` the second interpreter, `SKILL` the source skill directory, `COPY` its clean temporary copy, and `VALIDATOR` the environment's official skill-creator `quick_validate.py`. `EXPECTATIONS` is a temporary contract derived from the retained four-case, five-run, two-metric evaluation; `BASELINE` and `REMEDIATION` are the respective saved CSV files. These saved chapter files were used only for an additional regression check, not by the standalone suite or forward evaluators.

| Executed command/check | Actual result |
| --- | --- |
| `"$PY" -m unittest discover -s "$SKILL/tests" -v` | Exit 0; 15 tests passed in 3.068 seconds. |
| `"$PY" "$VALIDATOR" "$SKILL"` | Exit 0, `Skill is valid!`, after fixing an initially unquoted YAML description. |
| `"$PY" -m compileall -q "$SKILL/scripts" "$SKILL/tests"` | Exit 0. Generated bytecode was moved out of the distributed package. |
| `"$PY" -B -I -m unittest discover -s "$COPY/tests" -v` | Exit 0; 15 tests passed in 3.839 seconds from outside the book repository. |
| `"$PY312" -B -I -m unittest discover -s "$COPY/tests" -v` | Exit 0; the same 15 tests passed in 3.563 seconds. |
| `"$PY" "$COPY/scripts/check_eval_results.py" --help` | Exit 0; arguments, schema, limits and exit-code meanings displayed. |
| `"$PY" "$COPY/scripts/check_eval_results.py" --csv "$BASELINE" --expectations "$EXPECTATIONS" --dry-run` | Exit 0; header/contract valid, 40 metric rows expected, explicitly no data-row evaluation or gate verdict. |
| Same baseline command without `--dry-run` | Exit 1 as expected: 40 rows, two failed rows, zero case/metric count mismatches. |
| Same normal command with `--csv "$REMEDIATION"` | Exit 0; all 40 rows passed the explicit contract. No live rerun occurred. |
| YAML/JSON parsing, naming and file checks | Valid frontmatter and OpenAI metadata; implicit invocation enabled; 14 internal Markdown links resolved; exactly one `SKILL.md`; 13 package files; no empty directories, unfinished markers or embedded credentials found. |
| Python AST, standard-library `tabnanny`, whitespace/fence checks | Passed syntax, indentation, final-newline, trailing-whitespace and balanced-fence checks. |

Tests include the chapter-specific failure in which an acceptable average conceals two failed individual rows, exact row counts, changed thresholds, malformed and nonfinite data, lower-is-better criteria, sanitised diagnostics and repeated read-only execution. Deliberately invalid input fixtures test exits 1 and 2; those are successful regression assertions, not a claim every CSV passed.

No repository formatter/linter configuration was present. An offline attempt to obtain Ruff failed because it was absent from the local cache; Ruff was not run. The static and formatting checks listed above were performed, but are not a claim of full lint coverage.

## Six forward cases

Evaluators received no book or companion source and no expected patch. Their actual generated files and reports were reviewed. The routing evaluator handled three separate fixtures; interpreter metadata inspection was shared, but application contents remained separate. Reports and generated fixture applications stayed in the temporary validation workspace, outside the distributed skill.

| Realistic request | Observed result and boundary |
| --- | --- |
| Greenfield scheduling agent: prepare the smallest useful offline implementation and regressions | Created a domain-appropriate in-memory store, trusted-principal tool facade and 16 tests. The offline launcher passed 16/16 twice with zero skips and unchanged file hashes across execution. Preserved Python/ADK declarations. Explicitly stopped at a documented Runner integration plan because ADK was unavailable. |
| Custom inventory agent: diagnose earlier-turn verification permitting a later reservation | Preserved public APIs, dictionary schemas, dependency pins and `unittest`. Reproduced two failing regressions, then bound verification to principal/item/invocation with a two-line business-code change. All five tests passed. Rejections asserted no stored write; fresh verification restored success. This exercised real local inventory code, not ADK orchestration. |
| Missing credentials and prerequisites: assess live-evaluation readiness | Activated the skill and wrote a local plan; identified absent agent code, installed SDK and configuration. Did not invent credentials, claim ADC absence from environment variables alone, install packages or report live scores. |
| Managed evaluation with cloud and paid execution unapproved | Prepared an exact supplied project/region plan and clearly unexecuted preflight/API proposals. Kept unknown agent/scorer settings and budget unresolved, recorded required approvals and performed no remote operation. Live execution remained pending. |
| Ordinary temperature-converter tests | Declined ADK-skill activation based on its description, added exactly two standard-library tests and passed both. Introduced no ADK dependencies or configuration. |
| Second inventory invocation | Reinspected and reran five tests successfully. Made no additional code/configuration edits or duplicate tests; the parent independently verified all five source/configuration SHA-256 fingerprints unchanged. Only a new report was written. |

The forward trials establish useful local adaptation, scope selection and approval handling under the observed requests. They do not establish fresh ADK Runner/schema/provider compatibility, real authentication, durable or distributed writes, or a comprehensive adversarial assessment. The greenfield test's Python socket guard is not an OS-wide sandbox.

## Distribution and remaining limits

The standalone copy executed without the book, original evidence or companion packages. Automatic-discovery metadata is enabled, but the forward tests supplied the skill explicitly: marketplace installation, automatic product discovery and other coding-agent products were not smoke-tested. The core Markdown/helper does not require OpenAI metadata. Windows and Linux execution remain untested.

No provider calls, API activation, deployments, IAM changes, secrets, billable resources or cloud cleanup were performed to package this skill. Such operations still require the exact scoped approvals in `SKILL.md`. Only this new skill directory was added to the repository by this task; the original chapter manuscript and companion code were left unchanged. No commit, push, publication or global skill installation was performed.

## Depth review — 16 September 2026

Reopened the original and corrected manuscripts, current code and retained audit/remediation/cleanup evidence. Two independent source reviewers compared manuscript coverage and implementation/operational coverage. The resulting changes add three conditional references: `environment-and-troubleshooting.md`, `execution-evidence.md` and `evaluation-design.md`. Existing mode references now route to them and include concrete asset construction, offline test selection and score-export procedures. The entrypoint remains one skill; no new runtime dependency or executable helper was added.

### Repeated package checks

- Official `quick_validate.py`: exit 0, `Skill is valid!`.
- Clean-copy `python -B -I -m unittest discover -s "$COPY/tests" -v`: all 15 tests passed on CPython 3.11.4 in 3.069 seconds. An initial run through the host's default CPython 3.9.13 also passed all 15 in 4.934 seconds; this incidental result does not broaden the documented Python 3.11+ target.
- Same macOS 26.6.2 arm64 host. The explicit 3.11.4 validation environment retains PyYAML 6.0.3; the default-interpreter structural run used PyYAML 6.0. No package was installed for this review.
- Current package: 16 files, 28 valid internal Markdown links, one `SKILL.md`, implicit invocation enabled, no empty directories or bytecode artifacts. YAML/JSON parsing, Python AST/indentation, final newlines, whitespace, balanced fences and scoped credential/personal-path scans passed. Five shell code blocks passed `bash -n`; their cloud commands were **not executed**.
- `git diff --check -- skills/adk-agent-evaluation`: passed. All 38 original companion-source fingerprints still match the retained final manifest. No manuscript or companion application/test code changed.

### New scenario exercises

Two fresh agents received only the copied skill, separate synthetic fixtures and realistic requests, without source-review conclusions or an expected patch. Their turns were interrupted by an account usage limit. The parent reviewed the artifacts they had already written and completed local validation. A further design evaluation could not run independently and was completed as a parent walkthrough. These results are not three completed independent-agent passes.

| Scenario | Actual evidence |
| --- | --- |
| Resume evaluations after a project/key switch and activation timeout | The independent agent wrote a readiness plan and result report. Parent review confirmed it identified root-versus-local dotenv precedence, inherited key/server state, separate CLI/ADC/quota identities, unknown API state after permission denial, operation reconciliation before another activation, fresh target state and separate model/scorer readiness. It proposed commands without executing them or inventing scores. |
| Repair misleading observations under concurrent runs | The independent agent preserved the fixture's `summarise(data)` interface, corrected the summary and added six standard-library regressions. Parent execution on CPython 3.11.4 passed all six with zero skips. Replaying the original supplied function in memory against those tests produced five failures and one pass, as asserted by the comparison harness; no corrected source was overwritten. Confirmed effects were attributed to their originating run, cumulative/duplicate token records were counted once, and missing first-text timing remained null. |
| Design release evidence for a pending appointment and a lost acknowledgement | Parent walkthrough produced an ordered plan covering actual outcome assertions, commit-then-lost-response reconciliation, atomic shared model/tool reservations, fixed-trace judge calibration, contaminated holdout reclassification and staged release evidence. This was a design review only: no implementation, runtime test, live calibration or independent evaluator verdict is claimed. |

The observer fixture explicitly guarantees complete, ordered synthetic capture. Its passing parser tests do not establish that arbitrary live telemetry is complete or that local token counts equal billed cost. The readiness scenario supplied observations rather than real credentials. Temporary fixture applications/reports remain outside the skill package; the skill itself has no dependency on them.

No new ADK Runner execution, injected grading, conformance recording, cloud API request or deployed-service test occurred in this depth review. Detailed recipes retain their historical evidence labels; production budgets, judge calibration, holdouts, remote reconciliation and monitoring still require implementation and validation in the target project.
