interface:
  display_name: "ADK Memory Architecture"
  short_description: "Design and verify scoped agent memory and retrieval"
  default_prompt: "Use $adk-memory-architecture to inspect this agent and adapt its memory or retrieval design with meaningful validation."
policy:
  allow_implicit_invocation: true
